# Copy to .env and set for production (e.g. EC2).

# Port the server listens on
PORT=3000

# Comma-separated list of allowed CORS origins (e.g. your Amplify or CloudFront URL)
# Leave empty to allow all origins (dev only).
CORS_ORIGIN=
